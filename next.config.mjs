/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.sanity.io",
      "images.unsplash.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
